# Remote DB first (recommended)
DATABASE_URL=
USE_DOCKER_DB=0

# App ports
PORT_API=4000
PORT_WEB=3000
NEXT_PUBLIC_API_URL=http://localhost:4000

# Auth
JWT_SECRET=
JWT_EXPIRES_IN=7d
PASSWORD_PEPPER=

# Tracking
TRACKING_DEDUP_SECONDS=60
TRACKING_RATE_LIMIT_REQUESTS=30
TRACKING_RATE_LIMIT_WINDOW_SECONDS=300
TRACKING_FALLBACK_URL=https://example.com

# Docker Postgres settings (used only when USE_DOCKER_DB=1)
POSTGRES_DB=plataforma
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_PORT=5432
