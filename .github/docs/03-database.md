# 03 - Base de datos y migraciones

## Comandos
```bash
npm run db:migrate -w apps/api
npm run db:seed -w apps/api
npm run db:reset -w apps/api
npm run db:studio -w apps/api
```

## Notas
- `db:reset` es solo desarrollo.
- `db:migrate` aplica migraciones SQL versionadas.
