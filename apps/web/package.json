{
  "name": "web",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "rimraf build && cross-env CHOKIDAR_USEPOLLING=1 CHOKIDAR_INTERVAL=1000 next dev -p 3000",
    "build": "next build",
    "start": "next start -p 3000",
    "typecheck": "rimraf build && tsc --noEmit"
  },
  "dependencies": {
    "@stripe/react-stripe-js": "^5.6.0",
    "@stripe/stripe-js": "^8.7.0",
    "next": "^15.5.0",
    "react": "19.0.0",
    "react-dom": "19.0.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "@types/react": "^19.0.2",
    "@types/react-dom": "^19.0.2",
    "cross-env": "^7.0.3",
    "rimraf": "^6.1.2",
    "typescript": "^5.7.2"
  }
}
